import styles from './DogItem.module.scss';
import classNames from 'classnames/bind';

const cx = classNames.bind(styles);

function DogItem() {
    return <div>DogItem</div>;
}

export default DogItem;
